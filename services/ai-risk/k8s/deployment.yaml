apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-risk
  namespace: compound-trader
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-risk
  template:
    metadata:
      labels:
        app: ai-risk
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: ai-risk
          image: compoundtrader/ai-risk:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3007
          env:
            - name: PORT
              value: "3007"
            - name: SERVICE_NAME
              value: "ai-risk"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: redis-url
          readinessProbe:
            httpGet:
              path: /ready
              port: 3007
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ai-risk
  namespace: compound-trader
spec:
  selector:
    app: ai-risk
  ports:
    - name: http
      port: 3007
      targetPort: 3007
