apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-risk.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: ai-risk
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
