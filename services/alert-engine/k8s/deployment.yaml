apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-engine
  namespace: compound-trader
spec:
  replicas: 2
  selector:
    matchLabels:
      app: alert-engine
  template:
    metadata:
      labels:
        app: alert-engine
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: alert-engine
          image: compoundtrader/alert-engine:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3014
          env:
            - name: PORT
              value: "3014"
            - name: SERVICE_NAME
              value: "alert-engine"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: redis-url
          readinessProbe:
            httpGet:
              path: /ready
              port: 3014
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3014
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: alert-engine
  namespace: compound-trader
spec:
  selector:
    app: alert-engine
  ports:
    - name: http
      port: 3014
      targetPort: 3014
