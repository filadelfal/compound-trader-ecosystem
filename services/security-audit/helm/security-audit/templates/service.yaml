apiVersion: v1
kind: Service
metadata:
  name: {{ include "security-audit.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: security-audit
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
