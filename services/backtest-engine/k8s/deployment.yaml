apiVersion: apps/v1
kind: Deployment
metadata:
  name: backtest-engine
  namespace: compound-trader
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backtest-engine
  template:
    metadata:
      labels:
        app: backtest-engine
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: backtest-engine
          image: compoundtrader/backtest-engine:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3010
          env:
            - name: PORT
              value: "3010"
            - name: SERVICE_NAME
              value: "backtest-engine"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: redis-url
          readinessProbe:
            httpGet:
              path: /ready
              port: 3010
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3010
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: backtest-engine
  namespace: compound-trader
spec:
  selector:
    app: backtest-engine
  ports:
    - name: http
      port: 3010
      targetPort: 3010
