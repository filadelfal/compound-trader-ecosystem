apiVersion: v1
kind: Service
metadata:
  name: {{ include "journal-engine.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: journal-engine
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
