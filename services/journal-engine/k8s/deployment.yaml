apiVersion: apps/v1
kind: Deployment
metadata:
  name: journal-engine
  namespace: compound-trader
spec:
  replicas: 2
  selector:
    matchLabels:
      app: journal-engine
  template:
    metadata:
      labels:
        app: journal-engine
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: journal-engine
          image: compoundtrader/journal-engine:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3012
          env:
            - name: PORT
              value: "3012"
            - name: SERVICE_NAME
              value: "journal-engine"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: redis-url
          readinessProbe:
            httpGet:
              path: /ready
              port: 3012
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3012
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: journal-engine
  namespace: compound-trader
spec:
  selector:
    app: journal-engine
  ports:
    - name: http
      port: 3012
      targetPort: 3012
