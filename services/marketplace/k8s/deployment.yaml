apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketplace
  namespace: compound-trader
spec:
  replicas: 2
  selector:
    matchLabels:
      app: marketplace
  template:
    metadata:
      labels:
        app: marketplace
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: marketplace
          image: compoundtrader/marketplace:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3017
          env:
            - name: PORT
              value: "3017"
            - name: SERVICE_NAME
              value: "marketplace"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: redis-url
          readinessProbe:
            httpGet:
              path: /ready
              port: 3017
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3017
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: marketplace
  namespace: compound-trader
spec:
  selector:
    app: marketplace
  ports:
    - name: http
      port: 3017
      targetPort: 3017
