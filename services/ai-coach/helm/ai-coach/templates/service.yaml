apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-coach.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: ai-coach
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
