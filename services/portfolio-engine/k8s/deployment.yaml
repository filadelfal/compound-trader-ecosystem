apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-engine
  namespace: compound-trader
spec:
  replicas: 2
  selector:
    matchLabels:
      app: portfolio-engine
  template:
    metadata:
      labels:
        app: portfolio-engine
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: portfolio-engine
          image: compoundtrader/portfolio-engine:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3011
          env:
            - name: PORT
              value: "3011"
            - name: SERVICE_NAME
              value: "portfolio-engine"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: redis-url
          readinessProbe:
            httpGet:
              path: /ready
              port: 3011
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3011
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: portfolio-engine
  namespace: compound-trader
spec:
  selector:
    app: portfolio-engine
  ports:
    - name: http
      port: 3011
      targetPort: 3011
