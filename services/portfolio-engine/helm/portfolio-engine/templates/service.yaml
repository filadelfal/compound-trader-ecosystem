apiVersion: v1
kind: Service
metadata:
  name: {{ include "portfolio-engine.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: portfolio-engine
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
