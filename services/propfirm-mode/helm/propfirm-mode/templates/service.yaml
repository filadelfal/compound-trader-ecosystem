apiVersion: v1
kind: Service
metadata:
  name: {{ include "propfirm-mode.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: propfirm-mode
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
