apiVersion: apps/v1
kind: Deployment
metadata:
  name: propfirm-mode
  namespace: compound-trader
spec:
  replicas: 2
  selector:
    matchLabels:
      app: propfirm-mode
  template:
    metadata:
      labels:
        app: propfirm-mode
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: propfirm-mode
          image: compoundtrader/propfirm-mode:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3016
          env:
            - name: PORT
              value: "3016"
            - name: SERVICE_NAME
              value: "propfirm-mode"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: redis-url
          readinessProbe:
            httpGet:
              path: /ready
              port: 3016
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3016
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: propfirm-mode
  namespace: compound-trader
spec:
  selector:
    app: propfirm-mode
  ports:
    - name: http
      port: 3016
      targetPort: 3016
