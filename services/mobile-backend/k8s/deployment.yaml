apiVersion: apps/v1
kind: Deployment
metadata:
  name: mobile-backend
  namespace: compound-trader
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mobile-backend
  template:
    metadata:
      labels:
        app: mobile-backend
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: mobile-backend
          image: compoundtrader/mobile-backend:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3023
          env:
            - name: PORT
              value: "3023"
            - name: SERVICE_NAME
              value: "mobile-backend"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: redis-url
          readinessProbe:
            httpGet:
              path: /ready
              port: 3023
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3023
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: mobile-backend
  namespace: compound-trader
spec:
  selector:
    app: mobile-backend
  ports:
    - name: http
      port: 3023
      targetPort: 3023
