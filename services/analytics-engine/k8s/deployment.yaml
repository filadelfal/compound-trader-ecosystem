apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-engine
  namespace: compound-trader
spec:
  replicas: 2
  selector:
    matchLabels:
      app: analytics-engine
  template:
    metadata:
      labels:
        app: analytics-engine
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: analytics-engine
          image: compoundtrader/analytics-engine:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3015
          env:
            - name: PORT
              value: "3015"
            - name: SERVICE_NAME
              value: "analytics-engine"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: compound-runtime-secrets
                  key: redis-url
          readinessProbe:
            httpGet:
              path: /ready
              port: 3015
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3015
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-engine
  namespace: compound-trader
spec:
  selector:
    app: analytics-engine
  ports:
    - name: http
      port: 3015
      targetPort: 3015
